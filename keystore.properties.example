# Copy this to keystore.properties (gitignored) and fill in real values, or
# run scripts/setup-signing.sh to be prompted for them instead of hand-editing
# a file that holds your key password in plain text.
storeFile=/absolute/path/to/dugcanlift.jks
storePassword=
keyAlias=
keyPassword=
